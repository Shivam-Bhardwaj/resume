{
  "name": "resume-automation",
  "version": "1.0.0",
  "description": "Automated PDF generation for resume",
  "scripts": {
    "build:html": "node src/scripts/build.js all",
    "build:pdf": "node src/scripts/generate-pdf.js",
    "drc:check": "node src/scripts/validate.js",
    "drc:format": "npm run drc:check",
    "drc:build": "node src/scripts/assemble.js",
    "drc:critique": "echo 'Critique not yet ported to Node.js'",
    "sanity:content": "node src/scripts/sanity-content.js",
    "sanity:content:strict": "node src/scripts/sanity-content.js --strict",
    "build": "npm run drc:check && npm run drc:build && npm run build:html && npm run build:pdf",
    "build:recruiter": "npm run drc:build && node src/scripts/build.js recruiter && node src/scripts/generate-pdf.js recruiter",
    "build:founder": "npm run drc:build && node src/scripts/build.js founder && node src/scripts/generate-pdf.js founder",
    "build:tailored": "npm run drc:build && node src/scripts/build.js tailored && node src/scripts/generate-pdf.js tailored",
    "tailor": "node src/scripts/tailor.js --ai --build",
    "tailor:stdin": "node src/scripts/tailor.js --stdin --ai --build",
    "snapshot": "node src/scripts/snapshot.js",
    "archive": "node src/scripts/archive.js",
    "release": "npm run archive && npm run build"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "gray-matter": "^4.0.3",
    "handlebars": "^4.7.8",
    "marked": "^17.0.1",
    "puppeteer": "^23.0.0"
  }
}
