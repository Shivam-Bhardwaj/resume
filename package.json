{
  "name": "resume-automation",
  "version": "1.0.0",
  "description": "Automated PDF generation for resume",
  "scripts": {
    "build:html": "node src/scripts/build.js all",
    "build:pdf": "node src/scripts/generate-pdf.js",
    "drc:check": "cargo run -q --manifest-path cli/Cargo.toml -- check",
    "drc:build": "cargo run -q --manifest-path cli/Cargo.toml -- build",
    "drc:critique": "cargo run -q --manifest-path cli/Cargo.toml -- critique",
    "build": "npm run drc:check && npm run drc:build && npm run build:html && npm run build:pdf",
    "build:recruiter": "node src/scripts/build.js recruiter && node src/scripts/generate-pdf.js recruiter",
    "build:founder": "node src/scripts/build.js founder && node src/scripts/generate-pdf.js founder",
    "archive": "node src/scripts/archive.js",
    "release": "npm run archive && npm run build"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "puppeteer": "^23.0.0"
  }
}